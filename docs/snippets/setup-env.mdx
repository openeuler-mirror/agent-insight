{/* API Key 与环境变量配置说明 */}

在终端设置以下环境变量（推荐写入 `.env` 或 shell profile）：

```bash
export AGENT_INSIGHT_API_KEY="ai_xxxxxxxxxxxxxxxx"
export AGENT_INSIGHT_PROJECT="my-first-agent"
```

<Note>
  在 `AGENT WORKSPACE → Agent 管理` 页面可以创建 Agent 并获取 API Key。
</Note>
