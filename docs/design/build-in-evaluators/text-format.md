# 文本格式规范性评估器

## 目标

检查 Agent 输出的结构、标记和排版是否一致、可读、可渲染。

## 维度与计分

覆盖序号连续性、引用标记正确性、列表层级一致性、标点规范性、排版一致性、表格格式和特殊格式正确性七个维度。Judge 做离散判定，代码按 `0/20/50/80` 累计扣分并限制在 0–100；无列表、引用、表格或特殊格式时对应维度判 `safe`。

## 边界

代码块按代码语义处理，不检查代码命令中的编号；纯自然段无需强行套用标题或列表格式。
