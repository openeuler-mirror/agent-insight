# Phase 3：开发计划

1. 为 `ExperimentCase` 增加可空 `caseValuesJson` 字段并同步 Prisma Client。
2. 普通实验创建及追加 Case 时持久化数据集/Case 绑定。
3. 评测执行时读取绑定、复用有效缓存并将实时提取结果写回。
4. 增加 API 与实验引擎回归测试。
5. 更新用户指南和开发者数据流说明。
6. 在干净的 830 数据库上通过界面创建并执行实验，核对 Case 绑定与缓存写回。
