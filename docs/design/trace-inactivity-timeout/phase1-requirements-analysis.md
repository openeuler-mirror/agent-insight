# Trace 无上报超时：需求分析

状态：已按 2026-09-15 用户明确规则对齐；关联 Issue #301。

所有 Agent、所有采集框架，最后一次采集更新后连续 10 分钟没有新信息，统一触发兜底。不要求已生成回答，不再限定框架。明确结束信号优先。列表和详情应自动更新执行状态。

默认展示“超时”，与有明确退出失败证据的“失败”区分；超时后新信息到达可恢复执行中。超时不伪造 Session.endTime，也不因此触发评测。
