# 实验取消与评测服务停止：开发计划

1. 实现持久化停止标记、资源归属和受控执行取消，确保停止后不可自动恢复。
2. 实现评测机管理命令及离线镜像清理，与启动互斥，加入只读预览及部分失败报告。
3. 补齐平台持久化取消、各执行器接入、实验与 Case 删除接口、列表和汇总语义。
4. 增加页面操作和停止待确认展示，同步用户与开发指南。
5. 运行专项与全仓测试；浏览器和目标机器验证经用户确认后执行。

每阶段保留已有工作区改动，不操作实际部署资源。未完成的阶段在需求索引明确记录，不把基础接口实现视为端到端功能已完成。

## 实施记录

1～4 已落实至工作区；阶段 5 专项回归已通过，覆盖归属/幂等、取消先于启动、A/B 双侧映射、未确认执行、清理失败保留保护、离线清理和只读预览。已执行全量测试但未通过：当前测试库缺少部分 schema 字段，部分测试断言/夹具过期，Benchmark 执行器测试还出现挂起。不得用专项通过替代全仓与部署验收。

2026-09-23 验证记录：最终专项 152 项通过（含真实临时 Agent 子进程被取消）；Shell/Node 语法检查和 `git diff --check` 通过。全量一次运行 2921 项：2802 通过、106 失败、13 跳过；该次失败中两处接口变动断言已修复并在专项中重跑通过，未再次跑全量。类型检查仍有 136 个仓库既有测试/Agent RAS 错误，当前 `src/` 变动无类型错误。本次未声称全量通过。

剩余验收：同步新 schema 的隔离全量环境、浏览器 golden path + 运行中 Case 边界、Linux 真实 Docker 停服/共享引用/重复清理，以及远端客户端断线重连取消。Windows 定向进程树退出也需在目标系统验证。本次不启动用户 dev server，不变更用户数据库或实际 Docker 资源。
