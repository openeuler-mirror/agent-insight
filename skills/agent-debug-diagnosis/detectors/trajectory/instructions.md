# 循环与无进展诊断器

本诊断器只判断跨多个 turn 的时序问题：同一动作是否反复出现、执行是否长期没有引入新动作，以及轨迹是否缺少终止条件。

它不执行 AgentDebug 五模块分析，也不判断单个工具参数是否正确。输出中的区间、重复次数、比例和证据锚点是确定性事实；语义说明可由通用富化器润色，但不能改写这些事实。

定向查因仅在用户问题明确包含循环、反复调用、不收敛、无进展或无法停止等症状时匹配。
